/**
 * Game constants — magic numbers & config extracted to a single source of truth.
 */
import { DEFAULT_STATS as SHARED_DEFAULT_STATS } from '@games/stats-utils'

export const CPU_DELAY_MS = 400

export const DEFAULT_STATS = SHARED_DEFAULT_STATS
