/**
 * Bingo theme tokens - Centralized CSS custom properties
 * 
 * This package exports CSS variables for consistent theming.
 * Import the variables.css file in your styles to use them.
 * 
 * Usage:
 *   import '@games/bingo-theme-tokens/variables.css'
 *   
 *   // Then use in your CSS:
 *   color: var(--primary);
 *   background: var(--background);
 */

// Re-export as module for bundler integration
export {}
