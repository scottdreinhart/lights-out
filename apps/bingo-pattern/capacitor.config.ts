import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.scottreinhart.bingopattern',
  appName: 'Bingo Pattern',
  webDir: 'dist',
}

export default config
