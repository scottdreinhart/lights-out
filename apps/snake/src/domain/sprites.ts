/**
 * Re-export sprite utilities from shared assets package.
 */

export {
  getAllSpriteSources,
  getBackgroundCssValue,
  getThemeSprites,
  preloadAllSprites,
  preloadSprite,
  type SpriteAsset,
  type ThemeSpriteSet,
} from '@games/assets-shared'
