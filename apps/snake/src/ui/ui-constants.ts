/**
 * UI layout constants — sizes, breakpoints, timing values.
 *
 * BREAKPOINTS: Shared across all game apps via @games/ui-utils.
 * App-specific constants: Define locally per game requirements.
 */

export { BREAKPOINTS } from '@games/ui-utils'
