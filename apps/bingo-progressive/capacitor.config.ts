import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.scottreinhart.bingoprogressive',
  appName: 'Bingo Progressive',
  webDir: 'dist',
}

export default config
