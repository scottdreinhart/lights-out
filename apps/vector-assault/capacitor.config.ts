import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scottreinhart.vectorassault',
  appName: 'Vector Assault',
  webDir: 'dist'
};

export default config;
