import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.scottreinhart.patternbingo',
  appName: 'Pattern Bingo',
  webDir: 'dist',
}

export default config
