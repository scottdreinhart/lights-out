import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.scottreinhart.snakesandladders',
  appName: 'Snakes And Ladders',
  webDir: 'dist',
}

export default config
