import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scottreinhart.circuitmaze',
  appName: 'Circuit Maze',
  webDir: 'dist'
};

export default config;
