# useGame Companion Notes

- Holds the notes that were previously embedded as headers and helper commentary in `useGame.ts`.
- Purpose: keep the app helper focused on pure orchestration helpers and debug utilities.
- This companion is documentation-only and does not affect runtime behavior.
