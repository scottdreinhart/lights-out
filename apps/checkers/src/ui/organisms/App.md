# App Companion Notes

- Holds the notes that were previously embedded as section headers and inline commentary in `App.tsx`.
- Purpose: keep the organism source focused on render flow and wiring only.
- This companion is documentation-only and does not affect runtime behavior.
